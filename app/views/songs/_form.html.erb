<%= form_for @song do |f| %>
  <!-- <%= link_to 'New Song', new_artist_song_path(@song.artist, @song) if @song.artist %> -->
   <%= artist_select(params[:artist_id], @song) %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.submit %>
<% end %>
